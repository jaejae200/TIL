a = int(input()) #  비트단위(bitwise) 연산자는,
# ~(bitwise not), &(bitwise and), |(bitwise or), ^(bitwise xor),
# <<(bitwise left shift), >>(bitwise right shift)

print(~a) # 비트단위로 참/거짓을 바꿈 비트단위 연산자 ~ 이용 tilde 틸드